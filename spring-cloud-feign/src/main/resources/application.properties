spring.application.name=spring-cloud-feign
server.port=8094
#eureka.client.service-url.defaultZone= http://localhost:8090/eureka
#spring.thymeleaf.cache=false
#spring.thymeleaf.mode=LEGACYHTML5
#spring.thymeleaf.encoding=UTF-8
#spring.thymeleaf.servlet.content-type=text/html
#
##熔断器开启
#feign.hystrix.enabled=true
##feign集成了ribbon，所以实际开发中使用feign
##################################################################################(云配置在github，本地已不需要)
eureka.client.service-url.defaultZone= http://localhost:8090/eureka
spring.cloud.config.label=master
spring.cloud.config.profile=dev

#服务链路追踪
spring.zipkin.base-url= http://localhost:9411

#服务监控服务端口
spring.boot.admin.client.url=http://localhost:8101

# REDIS (RedisProperties)
# Redis数据库索引（默认为0）
spring.redis.database=0
# Redis服务器地址
spring.redis.host=127.0.0.1
# Redis服务器连接端口
spring.redis.port=6379
# Redis服务器连接密码（默认为空）
spring.redis.password=
# 连接池最大连接数（使用负值表示没有限制）
spring.redis.pool.max-active=8
# 连接池最大阻塞等待时间（使用负值表示没有限制）
spring.redis.pool.max-wait=-1
# 连接池中的最大空闲连接
spring.redis.pool.max-idle=8
# 连接池中的最小空闲连接
spring.redis.pool.min-idle=0
# 连接超时时间（毫秒）
spring.redis.timeout=5000


#Feign 是一个声明式的伪 Http 客户端，它使得写 Http 客户端变得更简单。使用 Feign，只需要创建一个接口并注解。它具有可插拔的注解特性，
# 可使用 Feign 注解和 JAX-RS 注解。Feign 支持可插拔的编码器和解码器。Feign 默认集成了 Ribbon，并和 Eureka 结合，
# 默认实现了负载均衡的效果
#
#Feign 采用的是基于接口的注解
#Feign 整合了 ribbon
